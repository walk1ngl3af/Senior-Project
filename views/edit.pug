extends layout

block content
    div
        p Use this link to turn files into links
        a(href='https://www.image2url.com/file-to-url' target='_blank') ImageToUrl
    br
    form(action=`/edit/${pet.id}` method='post')
        div.form-input
            label(for='name') Name:
            input(type='text' name='name' id='name' value=pet.name)
        div.form-input
            label(for='description') Description:
            input(type='text' name='description' id='description' value=pet.description required)
        div.form-input
            label(for='image') Image:
            input(type='text' name='image' id='image' value=pet.image)
        div.form-input
            label(for='location') Last Known Location:
            input(type='text' name='location' id='location' value=pet.location)
        div.form-input
            label(for='phone') Phone Number:
            input(type='text' name='phone' id='phone' value=pet.phone required)
        div.form-input
            label(for='date') Day the Pet was Lost:
            input(type='text' id='date' name='date' value=pet.date)
        div.form-input
            label(for='type') Type of Animal
            select(name ='type' id='type' autocomplete='off' required)
                option(value = "dog") Dog
                option(value = "cat") Cat
                option(value = "other") Other
        button.button(class="submit" type='submit') Submit
        a.button(class="found" href=`/delete/${pet.id}`) Report as Found
        a.button(class="cancel" href='/') Cancel